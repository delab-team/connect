import { DeLabConnect } from './core/Connect'
import { DeLabModal } from './core/Modal'
import { DeLabButton } from './core/Button'

import {
    DeLabEvent,
    DeLabNetwork,
    DeLabTypeConnect,
    DeLabAddress,
    DeLabError,
    DeLabConnecting,
    DeLabTransaction
} from './core/types'

import {
    DeLabScheme,
    DeLabModalConfig,
    DeLabButtonConfig
} from './core/types/react'

export { DeLabConnect, DeLabModal, DeLabButton }
export type {
    DeLabEvent,
    DeLabNetwork,
    DeLabTypeConnect,
    DeLabAddress,
    DeLabError,
    DeLabConnecting,
    DeLabTransaction,
    DeLabScheme,
    DeLabModalConfig,
    DeLabButtonConfig
}
